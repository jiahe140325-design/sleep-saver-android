# Sleep Saver 变更记录

> 规则：每次改动（设计稿、方案文档、技术方案）必须在此追加一条记录。
> 格式：版本 / 日期 / 执行者 / 改动内容 / 改动原因。
> 目的：多 Agent 协作时，这里是唯一的共享变更记忆。新 Agent 接手先读此文件。

---

## v1.1.1 — 2026-08-18 — 设计助手

**改动内容**

1. 在设计稿中新增「App Icon 方案」画板（node 10:1）
2. 替代 Codex AI 生成的复杂 3D 场景插画（床、窗帘、台灯、人物、饼干等多元素堆砌在圆角框内）
3. 新方案：深紫新月 + 奶油白闭眼（带睫毛）+ 茶绿四角星，奶油米黄底，圆角 22%
4. 提供 192/96/72/48dp 四尺寸预览，48dp 桌面最小尺寸仍可一眼辨认

**改动原因**：Codex 生图产生的图标元素过多，缩到 48dp 全糊；3D 粘土手办风与 App 扁平治愈手帐风完全割裂；改用单主体矢量图形，与 Sleep Saver 设计系统（深紫/奶油米黄/茶绿）完全一致

---

## v1.1 — 2026-08-17 — 设计助手（按 GPT 外部审核意见修正）

**改动内容**

1. 设计稿第 1 屏（今日）
   - meta 标签：「上床 / 醒来 / 睡前手机 / 夜里醒来」→「睡前打卡 / 早起打卡 / 睡前30分钟 / 夜间解锁」
   - 「睡前手机 42 分」→「睡前30分钟 · 24 分」（原值超过 30 分钟窗口，逻辑矛盾）
   - 目标时长 8h → 7h（与周目标 42/49h 对齐）
2. 设计稿第 2 屏（睡前打卡）
   - 倒计时「03:47 后睡觉」→「47 分钟后睡觉」（03:47 是剩余时长非时刻）
3. 设计稿第 3 屏（早起打卡）
   - 结算标题「昨晚结算 · 系统自动记录」→「昨晚记录 · 两次打卡之间」
   - 时间范围「上床 / 醒来」→「睡前打卡 / 早起打卡」
   - 夜间卡重做：删除用户自选次数（原与系统检测矛盾），改为仅系统客观结果「昨晚解锁 1 次 · 共用手机 6 分钟」+ 时间段明细 + 可修正入口
4. 方案文档新增：第 7 章技术方案要点（KEYGUARD_HIDDEN 判定、打卡后不锁屏漏记处理、早晨打卡 Session 排除、Session 合并、权限失效检测）、第 8 章 P0/P1/P2 优先级、第 9 章 17 项测试清单、第 11 章迭代记录

**改动原因**：GPT 外部审核指出 4 个设计逻辑问题 + 3 个隐性数据 BUG + 完整技术要求清单

---

## v1.0.1 — 2026-08-17 — 设计助手

**改动内容**

1. 第 1 屏 CTA「设置今晚睡眠提醒」（一次性语义）→「睡眠定时」完整设置卡：就寝 23:30 / 起床 08:00 双时间块 + 开关 + 「每晚重复 · 提前 30 分钟提醒」
2. 方案文档补充第 11 章「设计决策与迭代记录」

**改动原因**：用户反馈提醒应是固定规则（类 iOS 睡眠模式），不是每晚临时设置

---

## v1.0 — 2026-08-17 — 设计助手

**改动内容**：Android 版 5 屏设计稿全新建立（fileId 715804899549957 为早期 iOS 探索稿，Android 版为独立文件 715830098739096）

- 01 今日 / 02 打卡·睡前 / 03 打卡·早起 / 04 手帐 / 05 好友
- 安卓规范：412×915、安卓状态栏（品牌名 + 时间/WiFi/电池）、pill tab bar + 手势条
- 设计系统：奶油米黄 #FFF4D6 / 深紫 #3D3654 / 柔粉 #FFB6C1 / 茶绿 #7FA99B / 薰衣紫 #C9B6FF；Noto Serif SC + Fraunces + Outfit + Caveat
- 方案文档 v1.0 首发

**改动原因**：确定 Android 自用版先行（UsageStats 权限可实现真检测；iOS 受限；小程序后台冻结不可行）

---

## v0（探索稿）— 2026-08-17 — 设计助手

- iOS 风格 4 屏：今日 / 打卡 / 手帐 / 好友（fileId 715804899549957）
- 单次打卡思路，后因无法定义睡眠时长被 v1.0 双打卡闭环替代

---

## v1.2.0-dev.1 — 2026-08-17 — Codex（接手开发）

**改动内容**

1. 在本设计稿原目录建立原生 Android 工程，应用 ID 为 `com.sleepsaver.app`。
2. 技术基线：JDK 17、Gradle 8.13、AGP 8.13.2、Kotlin 2.3.21、compile/target SDK 36、min SDK 29。
3. 建立 Room `SleepSession` 数据库与双打卡状态机；重复睡前打卡会被拦截，5 分钟内可撤销，跨零点仍为同一 Session。
4. 实现 UsageStats 本地结算核心：`KEYGUARD_HIDDEN` 真解锁、Session 合并、早晨当前解锁排除、打卡后继续使用、权限失效时保留空值而不是写 0。
5. 实现今日、打卡、手帐、好友四个入口；好友页按用户决定改为「好友功能暂未开放」。
6. 实现 DataStore 睡眠定时、WorkManager 唯一提醒、通知权限、JSON/CSV 导出与关闭自动备份。
7. Manifest 不声明 `INTERNET`，数据只保存在本机；原始 UsageEvents 只在结算时读取，汇总后丢弃。

**改动原因**：WorkBuddy 额度用完后由 Codex 接手 V1 开发；用户确认好友功能暂缓、数据本地保存，并要求沿用原路径且完整记录每次迭代。

---

## v1.2.0-dev.2 — 2026-08-17 — Codex（构建兼容修正）

**改动内容**：将 Lifecycle 依赖从 2.11.0 调整为 2.10.0。

**改动原因**：首次编译验证确认 Lifecycle 2.11.0 要求 compile SDK 37 与 AGP 9.1；本项目采用稳定的 API 36 / AGP 8.13.2 基线，因此使用兼容版本，避免为一个依赖冒险升级整个构建系统。

---

## v1.2.0-dev.3 — 2026-08-17 — Codex（Compose 编译修正）

**改动内容**：将打卡页的 Android Context 提前在 Composable 范围获取，再交给授权按钮使用。

**改动原因**：第二次编译发现普通点击回调不能直接调用 `LocalContext.current`；调整作用域后行为不变，并满足 Compose 编译规则。

---

## v1.2.0-dev.4 — 2026-08-17 — Codex（测试覆盖）

**改动内容**

1. 新增 17 个本地自动化逻辑测试，对应方案文档第 9 章的 17 个场景。
2. 新增持久化安全策略常量，明确禁止 destructive migration、关闭 Auto Backup，并由测试锁定。
3. 将真机相关场景拆为「逻辑自动化」与「小米 14 Pro 装机验证」两层；自动化不冒充真机权限结果。

**改动原因**：首次成功构建时测试任务为 `NO-SOURCE`；必须补齐可重复运行的测试后，才能把核心检测逻辑称为已验证。

---

## v1.2.0-dev.5 — 2026-08-17 — Codex（首个可安装测试包）

**改动内容**

1. `testDebugUnitTest` 执行成功：17 项、0 跳过、0 失败、0 错误。
2. `assembleDebug` 构建成功，生成首个可安装测试 APK。
3. 静态检查确认 APK 不含 `INTERNET` 权限，应用关闭 Android Auto Backup。
4. 新增 `docs/TEST_REPORT.md`，逐项区分自动化结果与小米 14 Pro 真机待验结果。
5. ADB 设备列表为空，未擅自安装；release 签名因需要用户自行保管 keystore，暂不生成不安全的临时发布密钥。

**改动原因**：形成可追溯的首个交付点，同时诚实保留 HyperOS 真机权限、通知与真实解锁事件的验证边界。

---

## v1.2.1-dev.6 — 2026-08-18 — Codex（正式启动图标）

**改动内容**

1. 采用用户提供的极简 SVG：深紫新月、奶油色闭眼、茶绿色星星。
2. 背景统一为 `#FFF4D6`，圆角半径为画布边长的 22%。
3. 生成 `mdpi` 48px、`hdpi` 72px、`xhdpi` 96px、`xxhdpi` 144px、`xxxhdpi` 192px 五套 `ic_launcher.png`。
4. Manifest 显式配置 `android:icon` 与 `android:roundIcon`，替换安卓默认机器人及候选复杂场景插画。
5. 安装版本升级为 `versionCode 2` / `versionName 1.2.1-dev`，支持覆盖更新并保留本地数据。
6. 重新执行本地自动化测试：17 项通过、0 失败、0 错误，并完成 Debug APK 构建。
7. 更新 `docs/TEST_REPORT.md` 中的版本、真机边界、APK 大小与 SHA-256，保留完整交付记录。

**改动原因**：用户最终选择清晰、可缩放的 SVG 品牌符号，不采用复杂人物卧室插画作为桌面图标。

---

## v1.2.2-dev.7 — 2026-08-18 — Codex（原创玩偶打卡图标）

**改动内容**

1. 根据用户最终确认，启动图标改为原创无性别玩偶：佩戴睡眠帽、抱着枕头，并主动按下带勾的睡眠打卡键。
2. 保留天蓝色主视觉与奶油色、深蓝色辅助色；去除人物性别特征、动物特征和上一版新月闭眼符号。
3. 将 1254px 高清生成源图保存为 `design-assets/app-icon-v3-sleepcap-toy-source.png`，保留本次设计来源。
4. 更新图标生成脚本：统一应用 22% 圆角蒙版、清除源图黑色四角，并生成 1024px 归一化母版与五档 Android mipmap。
5. 安装版本升级为 `versionCode 3` / `versionName 1.2.2-dev`，允许在小米 14 Pro 上覆盖更新并保留本地数据。
6. 数据库、业务逻辑与权限声明均未修改；角色相似性筛查保留为未来公开发布前的品牌检查项。
7. 重新执行本地自动化测试与 Debug 构建：17 项通过、0 失败、0 错误，APK 内五档图标引用完整。
8. 更新 `docs/TEST_REPORT.md` 中的版本、玩偶图标说明、APK 大小与 SHA-256，完整记录本次交付结果。

**改动原因**：图标需要同时表达睡眠场景和用户主动建立规律作息的行为。玩偶提供亲和力，抱枕与睡眠帽说明场景，按下打卡键强调产品不是被动睡眠监测工具。

---

## v1.2.2-dev.8 — 2026-08-18 — Codex（GitHub 公开发布准备）

**改动内容**

1. 按用户确认，准备发布到公开仓库 `jiahe140325-design/sleep-saver-android`，仓库地址使用英文，介绍与版本说明使用中文。
2. 将 `README.md` 扩展为中文项目首页，补充产品定位、功能边界、隐私原则、小米安装步骤、界面预览、测试结果与构建方法。
3. 加强 `.gitignore`：排除 APK、签名密钥、环境变量文件、`secrets.properties`、本机 SDK 路径和编译目录。
4. 上传前扫描 50 个候选文件，未发现 API 密钥、密码、访问令牌或私钥；最大源码候选文件约 1.3 MB。
5. Debug APK 不提交到 Git 历史，改为单独上传到 GitHub Releases，避免仓库被大型二进制文件污染。
6. 当前不添加开源许可证：公开仅代表可查看，不自动授权复制、再发布或商业使用。
7. 已创建公开仓库：`https://github.com/jiahe140325-design/sleep-saver-android`，默认分支为 `main`。
8. 已创建中文预发布版本 `v1.2.2-dev`，并上传 `SleepSaver-1.2.2-dev.apk`；GitHub验证的文件大小和 SHA-256 与本地测试报告一致。

**改动原因**：将本地自用项目转换为可公开阅读、可追溯且不泄露本机配置的 GitHub 项目，同时保持源码历史与可安装产物分离。

---

## v1.2.2-dev.9 — 2026-08-18 — Codex（公开截图纠正）

**改动内容**

1. 新增 4 张小米 14 Pro 上的 `1.2.2-dev` 实际安装截图，分别覆盖今日、睡前打卡、睡眠手帐和好友暂未开放页面。
2. 将 `README.md` 的首要展示从早期设计图改为当前实机截图，并明确标注设备与版本。
3. 保留原始 5 张设计稿，但收纳到可折叠的「原始设计参考」区域，明确说明它们不是当前版本安装截图，也没有经过像素级还原验收。
4. 本次仅修正公开文档和截图归类，没有修改 App 源码、数据库、权限、版本号或 APK。

**改动原因**：GitHub 首页此前把早期设计稿标成「界面预览」，容易让人误以为它们就是当前 App 的实际界面。公开说明必须区分设计目标与已验证的真机结果，避免展示内容与安装体验不一致。

---

## v1.2.2-dev.10 — 2026-08-18 — Codex（README 项目故事）

**改动内容**

1. 根据用户的小红书介绍，在 `README.md` 开头新增第一人称的「为什么做它」项目故事。
2. 用睡前打卡、早起打卡和夜间解锁三个动作，轻松解释产品核心逻辑。
3. 补充不喜欢佩戴手环、希望观察睡前刷手机习惯的真实动机，以及 7 天、14 天、30 天长期自我实验计划。
4. 保留医疗边界、下载、隐私、测试和构建等专业说明；本次没有修改 App 源码、版本号或 APK。

**改动原因**：原 README 的信息准确但语气偏产品说明书，缺少项目发起人的真实动机与成长感。新的开头先让读者认识「为什么会有这个工具」，再进入功能和技术信息。

---

## v1.2.2-dev.11 — 2026-08-18 — Codex（安卓下载说明）

**改动内容**

1. 将 `README.md` 的「下载体验」改为「下载与安装」，明确本项目并非小米专用 APP。
2. 根据工程的 `minSdk 29` 标明支持 Android 10 及以上设备，同时如实说明目前只在小米 14 Pro / HyperOS 上完成实际安装验证。
3. 新增通用安卓安装步骤，并为下载 APK、进入安装页、安装完成和覆盖更新分别补充成功标志。
4. 明确提示不要下载 GitHub 的 `Source code` 压缩包，并保留 HyperOS 单次安装授权与 `.apk.2` 文件名修正方法。
5. 本次没有修改 App 源码、兼容范围、版本号或 APK，仅修正文档表述。

**改动原因**：原说明以小米安装步骤为主，容易让读者误以为 APP 只能在小米手机上使用。新的说明将「理论支持范围」和「已完成真机验证的范围」分开表达，既方便普通安卓用户安装，也不夸大兼容性结论。

---

## v1.2.2-dev.12 — 2026-08-18 — Codex（GitHub About 润色）

**改动内容**

1. 保持公开仓库名称 `sleep-saver-android` 不变，避免改变现有仓库链接。
2. 将 GitHub About 更新为：「🌙 给不爱戴手环的人做的睡眠打卡 APP：睡前按一下，早起再按一下，看看昨晚有没有偷偷刷手机。数据只保存在本机。」
3. 将 Website 设置为 `v1.2.2-dev` APK 发布页，方便访问者直接进入下载页面。
4. 新增 `android`、`kotlin`、`jetpack-compose`、`sleep-tracker`、`digital-wellbeing`、`local-first` 六个 Topics。
5. 已通过 GitHub 回读确认以上公开元数据保存成功；本次没有修改源码、README、Release 或 APK。

**改动原因**：原 About 表述准确但偏软件分类，缺少项目的个人动机和差异点。新的文案保留功能与本地隐私信息，同时用更轻松的方式说明产品使用场景。

---

## v1.3.0-dev.13 — 2026-08-19 — Codex（小手账打卡页）

**改动内容**

1. 按用户确认的第三版方向，将睡前打卡页重做为「个人小手账」：保留奶油米黄、深紫、柔粉、茶绿和薰衣紫配色，新增日期便签与原创睡眠帽玩偶横幅。
2. 将睡前和早起的主观状态统一为三档：「困得不行 / 还可以 / 挺精神」；旧记录仍按原文字保留，不修改 Room 表结构，也不做破坏性迁移。
3. 为三档状态与六个晚间事件加入 Material 图标、明确的选中边框和底色，替代原先连续的纯文字 FilterChip。
4. 将 JSON/CSV 导出从「今日」页移至「手帐」页底部，并更名为「数据管理」；导出格式、文件选择器和本地隐私边界保持不变。
5. 新增页面插画 `design-assets/check-in-journal-hero-source.png`，App 内使用 1200×600px 的 `drawable-nodpi/checkin_journal_hero.png`；启动图标不变。
6. 安装版本升级为 `versionCode 4` / `versionName 1.3.0-dev`，包名和数据库版本均保持不变，可覆盖安装并保留本地数据。
7. 重新执行 `testDebugUnitTest` 与 `assembleDebug`：17 项测试全部通过，0 跳过、0 失败、0 错误；APK 未声明 `INTERNET` 权限。
8. 更新 `docs/TEST_REPORT.md`，记录 APK 大小、SHA-256、三档兼容边界和真机复测步骤；新增 `design-qa.md` 记录当前因无连接设备而无法完成视觉截图对照。

**改动原因**：今日页堆叠低频导出入口削弱了首屏重点，打卡页连续纯文字选项也缺少情绪和陪伴感。新的信息架构让今日页专注当下、手帐页承接历史与数据管理，并用更贴近用户个人手账习惯的方式完成每日打卡。

---

## v1.3.1-dev.14 — 2026-08-19 — Codex（一屏时间轴今日页）

**改动内容**

1. 按用户最终确认的「方案 2 上半部分 + 方案 3 手账元素」混合稿，将今日页重做为一条从睡前到早起的纵向时间轴。
2. 时间轴依次显示睡前打卡、两次打卡之间的休息时长、早起打卡和夜间解锁；已完成、正在记录、尚无记录、使用数据不可用四种状态分别显示真实文案，不用未知值冒充 0。
3. 新增原创蓝色睡帽玩偶素材：生成母版保存为 `design-assets/today-timeline-mascot-source.png`，App 内使用 420×420px 的 `drawable-nodpi/today_timeline_mascot.png`。
4. 将本周目标压缩成票券式卡片，将「今晚放过自己」改为粉色手账便签，并把就寝时间、起床时间、提醒开关合并为一张紧凑定时卡。
5. 默认字体下按小米 14 Pro 的一屏内容区压缩垂直高度，同时保留 `LazyColumn`，在权限提示出现或系统字体放大时允许正常下滑，避免内容裁切。
6. 导出入口继续保留在手帐页底部，好友页仍为暂未开放；本次不改包名、Room 数据库结构、数据迁移和权限范围。
7. 安装版本升级为 `versionCode 5` / `versionName 1.3.1-dev`，可覆盖安装并保留本地记录。
8. 重新执行 `testDebugUnitTest` 与 `assembleDebug`：17 项测试全部通过，0 跳过、0 失败、0 错误；APK 未声明 `INTERNET` 权限。
9. 更新 `docs/TEST_REPORT.md` 与 `design-qa.md`；由于当前没有连接小米 14 Pro 或模拟器，今日页和打卡页的真机视觉对照仍如实标记为待验收。

**改动原因**：上一版今日页虽然功能齐全，但像多张功能卡直接堆叠，主线不清楚。新的时间轴先讲清「昨晚发生了什么」，再用小手账票券、便签和定时卡承接目标与行动，让首屏更有故事感，同时不牺牲真实数据边界和可操作性。

---

## v1.3.2-dev.15 — 2026-08-19 — Codex（睡眠手帐索引页）

**改动内容**

1. 按用户从三张设计图中选择的第 1 张方案，将手帐页改为「当月小结票券 + 最近七晚 + 纸张式历史记录」的手帐索引结构。
2. 将「数据管理」从历史列表最底部移动到页面右上角；点击后展开原有 JSON/CSV 导出操作，再次点击可收起，导出格式和系统文件选择器不变。
3. 当月小结动态统计记录晚数、平均休息时长和平均入睡时间；平均入睡时间对跨午夜场景做偏移计算，避免 23:30 与 00:30 被错误平均到中午。
4. 最近七晚改为按自然日排列的七列趋势，而不是仅展示最近七条记录；今天使用薰衣紫描边，有记录的日期使用茶绿时长柱，未记录日期保留空白。
5. 历史记录改为纸张式布局，突出日期与休息时长，并显示睡前到早起时间、醒来状态、首个事件标签以及手机使用摘要。
6. 手机使用数据为不可用或未记录时显示明确文案，不用 `0` 冒充未知值；旧记录中的状态文字与标签仍按原值展示，不修改数据库。
7. 将用户选中的完整页面保存为 `design-assets/journal-screen-v1.3.2-reference.png`；新增两张独立手帐资源 `journal_month_ticket.png` 和 `journal_history_paper.png`，高清母版分别保存在 `design-assets/journal-month-ticket-source.png` 与 `design-assets/journal-history-paper-source.png`。
8. 安装版本升级为 `versionCode 6` / `versionName 1.3.2-dev`，包名、Room 数据库版本和权限范围保持不变，可覆盖安装并保留本地记录。
9. 重新执行 `testDebugUnitTest` 与 `assembleDebug`：17 项测试全部通过，0 跳过、0 失败、0 错误；APK 未声明 `INTERNET` 权限。
10. 更新 `docs/TEST_REPORT.md` 与 `design-qa.md`；由于当前没有连接小米 14 Pro 或模拟器，本次 APK 标记为真机视觉验收包，不能用构建成功代替视觉通过。

**改动原因**：旧手帐页的普通柱状图和纯文字历史卡更像数据库结果，无法承接今日页与打卡页已经形成的小手账语言；同时导出入口会随记录增加不断下沉。新版先提供轻量月度回顾，再让每一晚像一张可翻阅的记录纸，在不增加睡眠评分或评判感的前提下提高可读性和连续使用的仪式感。

---

## v1.3.3-dev.16 — 2026-08-19 — Codex（今日页真机视觉修正）

**改动内容**

1. 接收并保存用户在小米 14 Pro 上安装 `1.3.2-dev` 后的今日页截图：`design-assets/qa/today-v1.3.2-xiaomi14pro.jpg`。
2. 将设计稿与真机截图归一到相同宽度并生成并排证据：`design-assets/qa/today-v1.3.2-reference-comparison.png`。
3. 确认两个 P1 问题：时间轴底部的锁图标和「0 次」被固定高度裁切；玩偶图片自带方形浅紫背景，破坏了趴在圆环边缘的叠放关系。
4. 不增加今日卡片总高度，将早起节点上移 17dp、夜间解锁节点上移 25dp，并同步缩短连接线，保证底部节点在原有一屏高度内完整显示。
5. 重新生成纯色背景的原创睡帽玩偶并进行色键透明处理，得到 512×465px 的 `design-assets/today-timeline-mascot-transparent.png`；App 内替换同名旧资源，圆环上的玩偶缩为 86dp 并重新定位。
6. 安装版本升级为 `versionCode 7` / `versionName 1.3.3-dev`；包名、Room 数据库、历史记录和权限范围不变，可覆盖安装。
7. 重新执行 `testDebugUnitTest` 与 `assembleDebug`：17 项测试全部通过，0 跳过、0 失败、0 错误；APK 未声明 `INTERNET` 权限。
8. 当前电脑仍没有连接真机或模拟器，修复结果必须由 `1.3.3-dev` 新截图确认；手帐页截图仍未收到，因此设计 QA 继续标记为 `blocked`。

**改动原因**：源码编译通过无法发现 Compose 固定高度在小米系统字体和实际密度下的裁切，也无法暴露位图背景与卡片底色之间的色差。本次根据真实安装截图修复可见问题，并把截图与对照过程纳入项目迭代记录。

---

## v1.3.4-dev.17 — 2026-08-19 — Codex（真机密度与票券留白修正）

**改动内容**

1. 接收并保存用户在小米 14 Pro 上的 `1.3.3-dev` 打卡页和手帐页截图：`design-assets/qa/checkin-v1.3.3-xiaomi14pro.jpg` 与 `design-assets/qa/journal-v1.3.3-xiaomi14pro.jpg`。
2. 分别与打卡页、手帐页设计真值生成同画布并排证据：`design-assets/qa/checkin-v1.3.3-reference-comparison.png` 与 `design-assets/qa/journal-v1.3.3-reference-comparison.png`。
3. 确认打卡页的空间问题是组件纵向密度叠加，不是信息数量过多；保留 3 个状态、6 个事件和主按钮，不删减任何打卡选项。
4. 将打卡页整体边距从 20dp 收紧为水平 14dp / 垂直 10dp，项间距从 16dp 收紧为 10dp；头部标题轻微缩小，保持原文案。
5. 打卡插画高度从 170dp 调整为 136dp；情绪按钮从 94dp 调整为 72dp，事件按钮从 70dp 调整为 54dp，主按钮从 54dp 调整为 50dp；所有可点击项仍不低于 48dp。
6. 月度票券的动态统计整体上移，增加左右安全边距和底部 28dp 留白；三组统计使用等宽分区，缩小字号并限制单行，避免「平均入睡」和数值挤到票券底边。
7. 安装版本升级为 `versionCode 8` / `versionName 1.3.4-dev`；包名、Room 数据库、历史记录、打卡逻辑与权限范围不变，可覆盖安装。
8. 重新执行 `testDebugUnitTest` 与 `assembleDebug`：17 项测试全部通过，0 跳过、0 失败、0 错误；APK 未声明 `INTERNET` 权限。
9. 生成易识别安装包 `app/build/outputs/apk/debug/SleepSaver-1.3.4-dev.apk`，SHA-256 为 `42b84fc005a10f4613de543aed5e84940ddfeafdd2beca15084c75404de2cfdf`。

**改动原因**：真机截图表明设计稿的首屏结构已经对了，但 Compose 实现把插画、选项和留白都做得偏高，导致最后的「开始记录」需要下拉才能看完；手帐票券则是文字定位没有尊重纸张的真实可见边界。本次优先修正密度和安全区，不用删减内容换取「伪一屏」。

---

## v1.3.5-dev.18 — 2026-08-19 — Codex（今日时间轴文字防重叠）

**改动内容**

1. 接收并保存用户在小米 14 Pro 上覆盖安装 `1.3.4-dev` 后的今日页截图：`design-assets/qa/today-v1.3.4-xiaomi14pro.jpg`。
2. 将今日页设计真值与该真机截图统一到 1200×2670px 画布，生成并排证据 `design-assets/qa/today-v1.3.4-reference-comparison.png`。
3. 确认「09:12」与「夜间解锁」不是字体缺字或裁切，而是两个通过 `offset` 绝对摆放的文字块仅相距 38dp，但真机字体的实际高度约为 40dp，因此必然发生覆盖。
4. 移除「早起打卡」和「夜间解锁」两组图标/文字的独立绝对坐标，新增 `TimelineEventRow`，改由同一个 `Column` 按两个 44dp 行与 4dp 间隔顺序排列，从结构上禁止两组信息互相覆盖。
5. 将底部节点图标从 45dp 收到 42dp，标签/数值明确设置为 12sp/18sp 和 15sp/22sp 行高，且限制单行；两行总高为 92dp，位于 276dp 内部可用高度内，底部仍留 2dp 安全区。
6. 为给结构化的两行节点留出空间，休息时长圆环从 126dp 收到 120dp，玩偶从 86dp 收到 82dp；今日主卡总高仍为 304dp，不把下方卡片推出首屏。
7. 安装版本升级为 `versionCode 9` / `versionName 1.3.5-dev`；包名、Room 数据库、历史记录、打卡逻辑与权限范围不变，可覆盖安装。
8. 重新执行 `testDebugUnitTest` 与 `assembleDebug`：17 项测试全部通过，0 跳过、0 失败、0 错误；APK 未声明 `INTERNET` 权限。
9. 生成易识别安装包 `app/build/outputs/apk/debug/SleepSaver-1.3.5-dev.apk`，大小 67,617,957 bytes，SHA-256 为 `55a165260849cb28456d5eb3e76762ea8cb1a5103bd5ed46c001ae083bbf6ec4`。

**改动原因**：`offset` 只改变元素的绘制位置，不会让相邻元素为它自动让出空间。上一次通过「上移坐标」解决了底部裁切，但把两组文字压到了一起。本次不再继续试坐标，而是换成会参与测量的行布局，修复问题的结构原因。

---

## v1.3.5-dev.19 — 2026-08-20 — Codex（GitHub 首页信息优化）

**改动内容**

1. 将当前版本、Android 支持范围、APK 直接下载、版本说明和完整更新记录集中到 README 首屏，减少访客寻找下载入口的步骤。
2. 新增「这一版做了什么」，概括今日时间线、小手帐打卡、月度小结与最近 7 晚等 `1.3.5-dev` 实际功能。
3. 更新核心功能列表，补充 3 种状态、6 种睡前事件、今日时间线、纸张式历史记录和 JSON / CSV 导出。
4. 将过度自我否定的项目描述调整为「仍在继续长大」，保留个人项目真实、轻松的表达。
5. 将 `1.2.2-dev` 截图归入「界面与版本演变」，明确它们不代表当前版本；在没有收到 `1.3.5-dev` 修复后完整实机截图前，不用设计稿或带已知问题的旧截图冒充新版效果。
6. 本次只修改公开文档，没有修改 App 源码、APK、版本号、数据库或权限范围。

**改动原因**：README 已有完整的项目故事与专业说明，但当前下载入口偏深、功能描述落后于最新迭代，早期截图也容易被误认成当前界面。本次优先让首次访问者快速理解「它是什么、这版做了什么、去哪里下载」，同时继续保持设计稿、旧截图和已验证实机效果之间的清晰边界。

---

## v1.3.5-dev.20 — 2026-08-20 — Codex（当前实机截图更新）

**改动内容**

1. 接收用户在小米 14 Pro / HyperOS 上安装 `1.3.5-dev` 后的四张最新截图，覆盖今日、睡前打卡、睡眠手帐和好友暂未开放页面。
2. 将原始截图按版本保存到 `docs/screenshots/actual/v1.3.5/`，使用不包含用户本机目录信息的英文文件名；不裁剪、不拼接，也不做二次压缩。
3. README 的「界面与版本演变」优先展示当前版本实机效果，四图使用统一显示高度，点击后可查看原始完整截图。
4. `1.2.2-dev` 的早期实机截图不删除，收纳到可折叠的历史区域，继续保留版本演变证据。
5. 本次只修改公开文档和截图，没有修改 App 源码、APK、版本号、数据库或权限范围。

**改动原因**：README 已经明确早期截图不代表当前界面，但访客仍然缺少 `1.3.5-dev` 的真实视觉证据。用同一天、同一设备上的四张最终实机截图替换首要展示，可以让 GitHub 首页与用户实际安装体验保持一致，同时不丢失早期版本的迭代记录。
---

## v1.3.6-dev.20 — 2026-08-21 — Codex（手帐趋势与悬浮详情）

**改动内容**

1. 按用户最终确认的手帐方向，将原本长期堆叠的睡眠记录收敛为「查看睡眠详情」入口；点击后在当前页面展开悬浮详情卡，不新增独立页面。
2. 详情卡一次只展示一晚记录，保留纸张、胶带和盖章视觉；可通过左右箭头和 3 个日期标签切换记录，收起后把主要空间留给趋势图。
3. 新增「平均睡眠时长」胶带贴纸柱状图：七个采样点使用不同柔和彩色胶带纹理，并保留用户目标睡眠时长参考线。
4. 新增「平均偏离计划」玩偶折线图：用原创睡眠帽玩偶头像作为数据点，明确显示「晚 1:08 / 早 0:18」等偏离方向，并标注计划附近的 ±30 分钟区域。
5. 新增统计时间段选择，默认近 7 周，可切换近 7 天、近 30 天、近 90 天或自定义起止日期；两张趋势图始终使用同一时间段，并自动切成最多 7 个等长统计区间。
6. 将历史记录中的「睡前使用」改为「本机睡前使用」，明确它只能说明这台安卓手机的行为，不能推断用户是否使用了另一部手机。
7. Room 数据库从 v1 升级到 v2：睡前打卡时快照保存当晚的计划就寝/起床时间，避免以后修改睡眠定时后，新的历史偏离统计随当前设置漂移；旧记录因没有历史计划快照，继续使用当前设置作为兼容回退。
8. 新增非破坏性的 `MIGRATION_1_2`，覆盖安装不会清空历史记录；本地 SQLite 迁移冒烟测试确认旧表记录保留，四个新增字段以可空列加入。
9. 安装版本升级为 `versionCode 10` / `versionName 1.3.6-dev`；重新执行 `testDebugUnitTest` 与 `assembleDebug`，17 项测试全部通过，0 跳过、0 失败、0 错误。
10. 生成易识别安装包 `app/build/outputs/apk/debug/SleepSaver-1.3.6-dev.apk`，大小 68,202,148 bytes，SHA-256 为 `a84caa6b4aee763df9a96d75139f4f97669ad59cb0706d62cd1966ce39e6e415`；静态检查确认仍未声明 `INTERNET` 权限。

**改动原因**：手帐页原本把每晚详情连续铺开，趋势数据反而缺少空间；同时「睡前使用几分钟」只能观察单台手机，不能代表用户完整的睡前行为。新版将长期观察重点调整为「睡了多久」和「实际睡前打卡偏离当晚计划多久」，用更轻松的手帐视觉表达趋势，再把单晚原始记录作为按需查看的证据层。

---

## v1.3.7-dev.21 — 2026-08-21 — Codex（手帐真机视觉返修）

**改动内容**

1. 将用户确认的三张局部效果图作为本轮视觉真值；小米 14 Pro 实装缺陷截图只保存在本地私有 QA 目录，不提交公开仓库。
2. 重做「平均睡眠时长」：缩短图表高度和胶带柱宽度，保留七种柔和纹理；为目标时长文案预留右侧空间，缺失日期只显示短横线，不再显示半透明幽灵柱。
3. 重做「平均偏离计划」：压缩卡片高度与标签尺寸，只连接相邻的真实数据点；缺失日期不再显示淡化玩偶，偏离标签固定宽度，避免「晚 2:39」等文字互相覆盖。
4. 重做「睡眠记录详情」：删除独立标题、描边按钮、菱形指示器和巨大白色外框，改为薰衣紫标题栏与纸张详情一体化展开；日期导航放在纸张下方，整体更接近已确认效果图。
5. 既有时间范围选择、偏离计划算法、Room v2 计划快照、睡眠记录和数据导出逻辑保持不变；本次不清库、不做 destructive migration。
6. 安装版本升级为 `versionCode 11` / `versionName 1.3.7-dev`；重新执行 `testDebugUnitTest` 与 `assembleDebug`，17 项测试全部通过，0 跳过、0 失败、0 错误。
7. 生成易识别安装包 `app/build/outputs/apk/debug/SleepSaver-1.3.7-dev.apk`，大小 67,765,409 bytes，SHA-256 为 `3e6feab8ccacf8bcc16ead04b100a5627b433b921ea10ba727908e3d3263f2fd`；静态检查确认仍未声明 `INTERNET` 权限。

**改动原因**：`1.3.6-dev` 虽然实现了正确的信息结构，但真机中把参考图的组件重新解释成了过高的胶囊柱、拥挤的玩偶折线和分散的详情模块，导致安装结果与确认稿差异过大。本次以同一视口的真实截图为问题证据，先修复可确定的布局和密度问题；最终视觉结果仍以小米 14 Pro 覆盖安装后的新截图为准。

---

## v1.3.8-dev.22 — 2026-08-21 — Codex（手帐整屏重构）

**改动内容**

1. 将用户最终确认的完整手帐页面保存为 `design-assets/journal-v1.3.8-full-screen-reference.png`；`1.3.7-dev` 小米 14 Pro 实装截图作为本地私有 QA 证据保留，完整确认稿成为本轮视觉真值。
2. 删除手帐主流程中的旧「8月小结」票券和旧统计骨架，按确认稿重建页面：顶部数据管理、近 7 晚 / 近 30 晚 / 自定义、日期范围与完成晚数、按日 / 按周 / 按月、两张趋势图、同页悬浮详情。
3. 统计不再固定切成最多 7 个等长区间：按日逐日统计、按周每 7 天统计、按月按自然月统计；点数超出屏幕时两张图都可横向滚动，完整保留所选时间段。
4. 新增七张真实透明纸胶带柱、格纹装饰胶带、波点装饰胶带和闭眼睡眠玩偶资源；平均睡眠时长不再使用代码绘制的圆角胶囊，偏离计划不再复用睁眼的今日页玩偶。
5. 「睡眠记录详情」改为居中的紧凑入口，默认在当前页展开带阴影的悬浮纸张卡；日期导航位于纸张上方，一次只显示一晚，可收起，不新建页面。
6. 数据库、Room v2 非破坏迁移、睡眠记录、计划快照、偏离计算、权限和导出格式保持不变；本次只重构手帐页面与视觉资源。
7. 安装版本升级为 `versionCode 12` / `versionName 1.3.8-dev`；执行干净构建，17 项单元测试全部通过，0 跳过、0 失败、0 错误。
8. 生成 `app/build/outputs/apk/debug/SleepSaver-1.3.8-dev.apk`，大小 70,070,805 bytes，SHA-256 为 `0970f78b831dc0ab19a6c8f63b3fc86ee2515b31f79fea38323bc9f18a8aa184`；静态检查确认仍未声明 `INTERNET` 权限。

**改动原因**：用户确认 `1.3.7-dev` 的问题不是局部字号或间距，而是实现继续沿用了旧页面骨架，导致真实安装版和完整确认稿像两个版本。本次不再在旧结构上修补，改为从整页信息架构、图形素材和展开状态重新实现。构建与逻辑测试已经通过，但视觉验收仍必须等待小米 14 Pro 在「自定义 + 按周 + 展开详情」同一状态下的新截图。

---

## v1.3.9-dev.23 — 2026-08-21 — Codex（偏离计划图真机瑕疵修正）

**改动内容**

1. 使用用户提供的 `1.3.8-dev` 小米 14 Pro 偏离计划局部截图完成本地对照；含真实日期与睡眠时间的缺陷证据不提交公开仓库。
2. 确认三个可见问题：最右侧偏离标签超出安全区被截断；图表纵向高度过大导致标题与数据之间留白失衡；「计划附近」仍是普通圆角框而不是确认稿的纸胶带。
3. 将图表左右安全边距从 26dp 增加到 34dp，保证首尾标签和 32dp 玩偶完整位于卡片内；横向最小点距调整为 44dp。
4. 将绘图区从 176dp 压缩到 148dp，基准线、折线振幅、纸带和横轴同步上移，减少无意义空白并接近确认稿比例。
5. 新生成并接入透明浅紫撕边纸胶带 `journal_plan_ribbon.png`，替换「计划附近 / ±30分钟」的普通圆角矩形；母版与可复现处理脚本保存在 `design-assets`。
6. 数据点、偏离值、缺失态、统计分组与历史计划快照算法均未修改；本次只修图表布局和视觉资源。
7. 安装版本升级为 `versionCode 13` / `versionName 1.3.9-dev`；17 项单元测试全部通过，APK 未声明 `INTERNET` 权限。
8. 生成 `app/build/outputs/apk/debug/SleepSaver-1.3.9-dev.apk`，大小 70,347,196 bytes，SHA-256 为 `0ee2cb41e88e838f3dd51bf4762d166e30ee7c466e42aa689ee4c91375ff106e`。

**改动原因**：`1.3.8-dev` 已完成整页结构重构，但真机局部图暴露了偏离计划组件在实际密度下的边缘裁切和纵向比例问题。修复以本次真机截图为证据，不再仅根据代码中的 dp 数值推断最终显示效果；`1.3.9-dev` 仍需同一区域复图才能完成视觉验收。

---

## v1.3.10-dev.24 — 2026-08-21 — Codex（偏离标签与玩偶防重叠）

**改动内容**

1. 使用用户提供的本地私有偏离计划局部放大图，确认「晚 1:17」时间便签压住睡眠玩偶帽子，而不是字体缺字。
2. 将时间便签从 22dp 高的圆角按钮感卡片收敛为 18dp 高的纸条式标签，字号从 9sp 调整为 8sp，圆角、阴影和边框同步减轻。
3. 重新以数据点中心对齐便签与玩偶：便签底部和玩偶图片顶部预留约 3dp 的固定间隔，七个数据点使用同一套规则，不再逐个试坐标。
4. 将折线基准位置下移 2dp、振幅从 16dp 收到 14dp，为极端偏离值的顶部标签保留安全区；偏离数值、计划快照、分组和缺失态算法均未修改。
5. 安装版本升级为 `versionCode 14` / `versionName 1.3.10-dev`；17 项单元测试全部通过，APK 未声明 `INTERNET` 权限。
6. 生成 `app/build/outputs/apk/debug/SleepSaver-1.3.10-dev.apk`，大小 70,347,204 bytes，SHA-256 为 `255744efd6727a2f90c401bd44d3ef38348f7bb7fd3b153717ad219bd50bd6b6`。

**改动原因**：上一轮修正了图表边缘裁切、整体高度和计划纸带，但时间便签与玩偶仍存在 9dp 左右的绘制重叠。新版本从组件尺寸与中心定位关系上消除重叠，并继续等待小米 14 Pro 同区域截图完成视觉验收。

---

## v1.3.11-dev.25 — 2026-08-22 — Codex（偏离计划整卡防重叠重构）

**改动内容**

1. 根据小米 14 Pro 最新局部截图重新检查整张「平均偏离计划」卡，确认 `1.3.10-dev` 虽然消除了便签与玩偶帽子的纵向覆盖，但相邻时间便签仍会横向互相遮挡。
2. 找到结构根因：旧标签宽 44dp，七点图在当前视口中的实际中心间距只有 40dp，标签无论怎样微调坐标都会天然重叠 4dp；后绘制的右侧标签还会覆盖左侧文字。
3. 不再继续试单个坐标。图表最小宽度改为「左右安全区 34dp × 2 + 相邻点固定间距 48dp × (点数 - 1)」，超出屏幕时沿用横向滚动；标签宽度收为 42dp，任意相邻标签至少保留 6dp 可见间隔。
4. 时间标签不再使用代码绘制的圆角卡，改为真实奶油色纸便签 PNG：近方角、轻阴影、底部居中纸尖；透明资源为 `journal_deviation_label.png`，生成母版与可复现处理脚本保存在 `design-assets`。
5. 标签高度固定为 25dp、字号 8sp / 行高 9sp；标签底部与 32dp 玩偶顶部保留 3dp 固定安全间隔。首尾点继续使用 34dp 安全区，避免标签与玩偶被卡片边缘裁切。
6. 新增第 18 项回归测试：对 2—31 个点逐一验证 48dp 点距、至少 6dp 横向标签间隔，以及 3dp 标签—玩偶纵向间隔，防止同类碰撞再次回归。
7. 睡眠数据、偏离计划算法、统计分组、Room v2 数据库、历史记录和权限范围均未修改；本次只重构标签资产与图表布局约束。
8. 安装版本升级为 `versionCode 15` / `versionName 1.3.11-dev`；`testDebugUnitTest` 与 `assembleDebug` 通过，18 项测试全部通过，0 跳过、0 失败、0 错误。
9. 生成 `app/build/outputs/apk/debug/SleepSaver-1.3.11-dev.apk`，大小 70,965,654 bytes，SHA-256 为 `a176b11ba9a1147b7375a7bc67c5e20bf9aa9c5fcf62aebc3da73582c7e4abc2`；静态权限检查确认未声明 `INTERNET`。

**改动原因**：连续两轮局部坐标调整没有覆盖「标签自身宽度大于数据点间距」这一横向约束，导致同一类重叠问题反复出现。本次把最小点距、标签宽度、边缘安全区和纵向安全区一起固化为可测试的不变量；构建与逻辑回归已完成，但最终视觉仍需小米 14 Pro 同区域截图确认。

---

## v1.3.12-dev.26 — 2026-08-22 — Codex（近 7 晚一屏与单次日期范围确认）

**改动内容**

1. 使用 `1.3.11-dev` 小米 14 Pro 本地私有真机截图确认近 7 晚首次打开时最右日期、便签和玩偶只显示一部分，手动横向拖动后才能完整看到。
2. 找到右侧裁切根因：上一版为了固定 48dp 点距，将七点图强制扩大到 356dp；实际可视区域小于 356dp，而 `rememberScrollState()` 默认从最左侧偏移 0 开始，所以最新一天必然落在初始视口外。
3. 将偏离图拆成两种明确布局：7 个点以内且视口不小于 264dp 时，图宽直接等于可视宽度，不产生横向滚动；超过 7 个点或视口不足时才扩展图宽，并在首次布局后停靠最右端，优先显示最新记录。
4. 近 7 晚一屏模式使用 18dp 首尾安全区、34dp 纸便签宽度与至少 4dp 标签间隔；长区间使用固定 42dp 点距与 8dp 标签间隔，继续支持左右滑动。
5. 自定义日期从两个连续的 Android 系统日期弹窗，替换为单个 Material 3 日期范围选择器：先点击开始日期，再点击结束日期，所选范围由系统连续着色，最后只点击一次「确认」应用筛选。
6. 只有开始和结束日期都存在时「确认」按钮才可用；取消时保留原筛选。日期通过 UTC 日历边界转换，避免不同时区导致日期前后偏移。
7. 新增第 19 项回归测试，验证日期范围未选完整时不可确认、选齐后可一次确认，以及 LocalDate 与日历毫秒值能无损往返；第 18 项同步改为覆盖 264dp 小屏一屏模式和 8—31 点长区间滚动模式。
8. 睡眠数据、偏离算法、统计口径、Room v2 数据库、计划快照和导出格式均未修改；本次只修手帐筛选交互与偏离图布局。
9. 安装版本升级为 `versionCode 16` / `versionName 1.3.12-dev`；`testDebugUnitTest` 与 `assembleDebug` 通过，19 项测试全部通过，0 跳过、0 失败、0 错误。
10. 生成 `app/build/outputs/apk/debug/SleepSaver-1.3.12-dev.apk`，大小 70,506,079 bytes，SHA-256 为 `b71b42f4b0839644b5b99af3cf348dfbd895afe2cb9c99375b6b98ad44b705bf`；静态权限检查确认未声明 `INTERNET`。
11. 用户确认本轮优化可以同步至公开 GitHub；公开提交包含源码、Room v2 架构、运行所需视觉资源、可复现设计母版与测试文档，新版真机 QA 截图因包含个人日期和睡眠时间只保存在本地。

**改动原因**：近 7 晚本来就是单屏摘要，不应该为了防标签重叠而变成默认裁掉最新一天的滚动图；自定义日期也应该在一个连续上下文中完成范围选择，而不是让用户确认两次。本次按「短范围完整可见、长范围默认看最新、日期范围一次提交」三个交互不变量重构并补测试，最终视觉与范围着色仍需小米 14 Pro 安装截图确认。

