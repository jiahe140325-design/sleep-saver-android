# Sleep Saver v1.4.1-dev 设计对照

## 本轮确认目标：纠正记录单层编辑器

- 用户选定的方案 3 确认稿已保存为 `design-assets/session-time-editor-v1.4.1-reference.png`。
- 最近一晚的入口前置到今日页和打卡完成态；历史入口前置到手帐顶部，先选择晚次，再打开同一个编辑器。
- 编辑器必须在一层 Compose 弹窗内同时展示准备休息和实际起床；日期使用左右箭头调整，小时与分钟使用 24 小时制数字输入，并提供 `−1时`、`−10分`、`+10分`、`+1时` 快捷按钮。
- [代码已实现] 三个入口、异常时长突出提示、历史晚次选择器和共享单层编辑器均已接入；测试覆盖直接输入、非法 24/60、跨午夜快捷调整和异常时长识别。
- [视觉待验] 当前没有连接小米 14 Pro 或安卓模拟器，尚无同状态渲染截图可以和确认稿并排比较。弹窗高度、数字键盘遮挡、小米字体换行、按钮间距与触摸命中不能只靠源码或单元测试判定通过。

## 本轮五项检查

- 字体与排版：确认稿要求两组时间字段不拥挤、数值清晰；当前代码使用统一数字字段，真机字体与键盘状态待截图。
- 间距与布局：入口均为完整宽度或明显卡片；单层弹窗可滚动，仍需验证小米 14 Pro 首屏与键盘弹出后的保存按钮。
- 色彩：沿用奶油底、浅紫卡、深紫文字、薄荷绿确认色和粉色异常提示，不新增另一套视觉语言。
- 交互：最近一晚一步进入；历史记录两步进入（选择晚次 → 编辑），不再要求先展开睡眠详情；编辑时间不再嵌套系统日期和时钟弹窗。
- 文案与内容：入口明确写出「纠正记录 / 纠正昨晚记录 / 纠正历史记录」，异常状态说明「这条记录可能有误」，避免只用铅笔图标暗示功能。

## 本轮构建证据

- 版本：`versionCode 19` / `versionName 1.4.1-dev`。
- 自动化：32/32 通过，0 跳过、0 失败、0 错误；Android Lint 与 Debug 构建通过。
- APK：`app/build/outputs/apk/debug/SleepSaver-1.4.1-dev.29.apk`。
- 大小：70,719,071 bytes。
- SHA-256：`6b46b7a0271de706f7de4868a970eaa693552cb3534feca74cf1a70982f1f23e`。
- 权限：静态检查未声明 `INTERNET`。

## 历史基线：v1.3.12

## 本轮视觉真值与实机证据

- 完整确认稿：`design-assets/journal-v1.3.8-full-screen-reference.png`，852×1846px。
- `1.3.11-dev` 小米 14 Pro 右侧裁切截图作为本地私有 QA 证据保留，不提交公开仓库。
- 实机证据显示近 7 晚首次打开时最右日期、纸便签和玩偶只显示一部分；手动向左滑动内容后才完整。
- 自定义日期目标交互由用户明确确认：一个日历内先选开始日期、再选结束日期，范围着色，最后只点一次「确认」。

## 两个结构问题

- [P1 已定位] 近 7 晚被错误当成长区间滚动图。
  - `1.3.11-dev` 将七点最小图宽固定为 356dp，大于小米当前可视宽度。
  - `rememberScrollState()` 初始偏移为 0，所以页面默认展示左侧，最新一天落在视口右侧之外。
  - 手动拖动能看到完整内容，证明数据和标签本身存在，问题在默认布局与滚动停靠。
- [P1 已定位] 自定义日期使用两个嵌套系统弹窗。
  - 第一次「确定」只提交开始日期并打开第二个弹窗；第二次「确定」才提交结束日期。
  - 用户无法在同一日历中看到完整范围，也没有连续范围着色，交互与日期范围选择的预期不一致。

## v1.3.12 修复规则

- [已实现 / 待真机] 7 个点以内且可视宽度不小于 264dp时，偏离图宽度直接等于视口，不启用横向滚动。
- [已实现 / 待真机] 一屏模式使用 18dp 首尾安全区、34dp 纸便签和至少 4dp 标签间隔；最右一天必须初始完整可见。
- [已实现 / 待真机] 超过 7 个点或视口不足时才扩展图宽；长区间固定 42dp 点距、8dp 标签间隔，并在布局完成后默认停靠最右端显示最新记录。
- [已实现 / 待真机] 两个 `android.app.DatePickerDialog` 已替换为单个 Material 3 `DateRangePicker`。
- [已实现 / 待真机] 点击新开始日期后等待结束日期，开始与结束都存在时「确认」才可用；确认一次后应用范围，取消则保留旧筛选。
- [不变] 睡眠数据、偏离算法、计划快照、分组统计、Room v2 数据库和导出格式均未修改。

## 五项检查

- 字体与排版：纸便签缩到 34dp，文字使用 7.5sp / 8.5sp 单行居中；小米字体下是否仍清晰待截图确认。
- 间距与布局：自动化已验证 264dp 小屏七点一屏、8—31 点长区间点距和标签安全间隔；实机首屏仍待确认。
- 色彩：沿用奶油底、浅紫卡、深紫文字、奶油纸便签和蓝色睡帽玩偶，没有新增配色。
- 交互：日期范围选择已变为单弹窗、一次确认；范围着色由 Material 3 日期范围组件提供，真机触摸流程待复测。
- 文案与内容：「选择统计日期 / 再选择结束日期 / 已选择日期范围 / 确认 / 取消」均为中文；真实日期和偏离值未写死。

## 构建证据

- 实现：`app/src/main/java/com/sleepsaver/app/ui/SleepSaverApp.kt`。
- 回归测试：`app/src/test/java/com/sleepsaver/app/SleepSaverLogicTest.kt` 第 18、19 项。
- `./gradlew testDebugUnitTest assembleDebug`：通过。
- 自动化测试：19/19 通过，0 跳过、0 失败、0 错误。
- APK：`app/build/outputs/apk/debug/SleepSaver-1.3.12-dev.apk`。
- 版本：`versionCode 16` / `versionName 1.3.12-dev`。
- 大小：70,506,079 bytes。
- SHA-256：`b71b42f4b0839644b5b99af3cf348dfbd895afe2cb9c99375b6b98ad44b705bf`。
- 权限：未声明 `INTERNET`。

## 当前证据边界

- 用户已确认本轮优化可以同步至 GitHub；本版本按开发预发布版本交付。
- 当前 ADB 没有连接设备，也没有可用安卓模拟器，最新同区域真机截图未纳入公开仓库。
- 单元测试能证明宽度分支、标签盒子间距、确认按钮条件与日期转换逻辑，但不能证明小米上的纸条文字、日期范围着色、弹窗尺寸和首次滚动位置已经视觉正确。
- 因此可以发布 `1.3.12-dev` 开发预览，但不把自动化通过表述成多设备或像素级真机验收。

## 小米 14 Pro 复测步骤

1. 覆盖安装 `SleepSaver-1.3.12-dev.apk`，不要卸载旧版。
   - 成功标志：旧睡眠记录仍存在。
2. 打开「手帐 → 近 7 晚」，不要进行任何横向拖动。
   - 成功标志：从最左到最右七个日期都完整；最新日期、纸便签和玩偶不被裁切。
3. 点击「自定义」。
   - 成功标志：只出现一个日期范围日历，不再连续弹两个系统日期框。
4. 点击一个开始日期，再点击一个结束日期。
   - 成功标志：两个端点及中间日期范围连续着色；选完后只点击一次「确认」即可回到手帐并更新日期范围。
5. 选择超过 7 个统计点的范围。
   - 成功标志：首次打开默认显示最右侧最新记录，可向右滑动内容回看更早日期。
6. 提供近 7 晚完整偏离卡与日期范围选择完成态两张截图，再和确认稿做同视口比较。

## 比较历史

- `1.3.10-dev`：只修便签与玩偶的纵向关系，遗漏横向标签宽度。
- `1.3.11-dev`：通过扩宽图表消除标签覆盖，但使近 7 晚产生横向滚动，默认裁掉最新一天。
- `1.3.12-dev`：短范围改为一屏布局，长范围才滚动并默认看最新；自定义日期改为一个范围日历、一次确认。

final result: blocked — current Android render is unavailable for same-state comparison with the selected reference
